<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- External Storage - Models persist after app uninstall -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <!-- Queries for AI Core -->
    <queries>
        <package android:name="com.google.android.aicore" />
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.ConversationalAI"
        android:allowBackup="true"
        android:supportsRtl="true">

        <!-- Cancel Download Receiver -->
        <receiver
            android:name=".CancelDownloadReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.niquewrld.conversationalai.ACTION_CANCEL_DOWNLOAD" />
                <action android:name="CANCEL_DOWNLOAD" />
                <action android:name="PAUSE_DOWNLOAD" />
                <action android:name="RESUME_DOWNLOAD" />
            </intent-filter>
        </receiver>

        <!-- Model Download Service -->
        <service
            android:name=".service.ModelDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Chat Setting Activity -->
        <activity
            android:name=".ChatSettingActivity"
            android:exported="false" />

        <!-- Chat History Activity -->
        <activity
            android:name=".ChatHistoryActivity"
            android:exported="false" />

        <!-- Policy Activity -->
        <activity
            android:name=".PolicyActivity"
            android:exported="false" />

        <!-- Splash Screen (Launcher Activity) -->
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize|stateHidden" />

    </application>

</manifest>
